/**
 * 统一的加载组件导出
 * 这个文件导出所有与加载状态相关的组件
 */

import Skeleton, {
  RecipeListSkeleton,
  RecipeSkeleton,
  RecipeGridSkeleton,
  SearchSkeleton
} from '../ui/SkeletonLoader';
import WithSkeleton from '../ui/WithSkeleton';

// 骨架屏组件
export {
  Skeleton,
  WithSkeleton,
  RecipeListSkeleton,
  RecipeSkeleton,
  RecipeGridSkeleton,
  SearchSkeleton
}; 